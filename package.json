{
  "name": "dreamcatcher-python",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "curl http://localhost:4000/api/v1/openapi > openapi.json && openapi-generator-cli generate -i openapi.json -g python-pydantic-v1 -o . -c config.yaml --skip-validate-spec"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.13.4"
  }
}
